{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "rsih-skill-evolution.shared.projection-watermark.v1.schema.json",
  "title": "Contract 7.14 ProjectionWatermark",
  "description": "CTR-005 machine-readable freeze of System Contract ['system-contract.md 7.14']. Closed JSON Schema subset: additionalProperties false, explicit enums, integer-only numbers, conditional required fields via if/then. Hand-written file; the source of truth for the Go Host / Go GMS / TS RSIH adapters (HST-101/GMS-101/RSI-101).",
  "contract_refs": [
    "system-contract.md 7.14"
  ],
  "$ref": "../explore-result.schema.json#/$defs/projection_watermark",
  "x-digest": {
    "digest_field": "watermark_digest",
    "algorithm": "sha256-over-jcs",
    "preimage_fields": [
      "schema_version",
      "projection_stream",
      "projection_schema_version",
      "projection_head",
      "projected_through_activation_sequence",
      "source_ledger_digest",
      "state"
    ],
    "preimage_rule": "watermark_digest = SHA-256(JCS(core minus watermark_digest)); this DTO has no extensions field."
  }
}
