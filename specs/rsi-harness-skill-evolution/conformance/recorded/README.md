# FND-002 recorded corpus

Frozen Host Sealed Segment fixtures (segments/) and RSIH Q29-B fake runtime packets (q29b/). Pure data: no executable instructions. Validate with

    python3 validate_recorded.py --root recorded

from the conformance/ directory.
